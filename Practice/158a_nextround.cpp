#include <iostream>
#include <algorithm>
using namespace std;
int main(){

  int a,key;
  cin>>a>>key;
  int arr[a];
  cout<<"Enter the elements";
  for(int i=0; i<a; i++){
    cin>>arr[n];
   } 
  int n=sizeof(arr)/sizeof(int);
   
   
   // Binary Search function
  auto it=upper_bound(arr,arr-a,key);
  return 0;
}
